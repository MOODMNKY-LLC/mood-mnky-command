# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
instrumentation-client.js
instrumentation-client.ts

# Common ignores
remote_only_versions.txt
node_modules/
# Commit .cursor except mcp.json (project-specific MCP config, often has local paths/keys)
.cursor/mcp.json
.cursor/mcp.json.tmp
.cursor/mcp.json.backup
.next/
.turbo/
.env
.env*.local
.env.tmp
.env.production
.env*.backup
.env.master
.DS_Store
.pnpm-store/
temp/
voice-previews/

# Serwist
public/sw*
public/swe-worker*
.vercel

# Pulled theme copies (reconciliation only; do not commit)
Shopify/theme-live/
Shopify/theme-dev/

# Mintlify docs: exclude archive and env reports that may contain credential examples
mnky-docs/archive/
mnky-docs/env-management/

# Local TLS certs (mkcert-generated for Supabase)
certs/
apps/web/certificates/
