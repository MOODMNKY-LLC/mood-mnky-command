# MNKY MEDIA – Jellyfin image
# Base official image; theme CSS served via CDN or injected at runtime.
FROM jellyfin/jellyfin:latest

# Optional: copy theme from build context when building with publish script.
# COPY mnky.css /config/data/

EXPOSE 8096
