# =============================================================================
# Discord Agent Bots Stack — Copy to .env and fill for docker compose or local run.
# Tokens: Notion Credentials (MOOD MNKY Discord Bot Token, SAGE MNKY, CODE MNKY).
# =============================================================================

# Required: one per bot (Discord Developer Portal → Applications → Bot → Token)
MOOD_MNKY_DISCORD_BOT_TOKEN=
SAGE_MNKY_DISCORD_BOT_TOKEN=
CODE_MNKY_DISCORD_BOT_TOKEN=
MNKY_VERSE_DISCORD_BOT_TOKEN=

# Required: web app internal API key (POST /api/discord/events, profile-by-discord-id, agent-reply)
MOODMNKY_API_KEY=

# Required: web app base URL. From inside Docker, use host.docker.internal to reach the host:
#   Local (app on host:3000): VERSE_APP_URL=http://host.docker.internal:3000
#   Production: VERSE_APP_URL=https://mnky-command.moodmnky.com
# Do NOT use http://localhost:3000 in Docker — localhost is the container, not the host.
VERSE_APP_URL=

# Required: Redis URL (Compose: redis://redis:6379; local: redis://localhost:6379)
REDIS_URL=redis://redis:6379

# Optional: if bots call OpenAI directly instead of agent-reply API
# OPENAI_API_KEY=

# Optional: if bots query Supabase directly for profile (else use web app profile-by-discord-id)
# SUPABASE_SERVICE_ROLE_KEY=
# NEXT_PUBLIC_SUPABASE_URL=

# Optional: n8n webhook URLs for workflows triggered by Discord
# N8N_WEBHOOK_URL_WELCOME=
# N8N_WEBHOOK_URL_XP_NOTIFY=

# Optional (Windows only): if HTTPS to production fails with certificate revocation errors, set to 0
# so Node can complete TLS. Use only in a trusted environment. See docs/DISCORD-BOTS-ENV.md.
# NODE_TLS_REJECT_REVOCATION=0
