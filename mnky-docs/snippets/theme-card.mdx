export function ThemeCard({ title, description, image, href }) {
  return (
    <a href={href} className="no-underline">
      <div className="rounded-xl border border-gray-100 dark:border-gray-800 bg-white dark:bg-gray-900 shadow-sm p-5 mb-6 transition-all hover:shadow-md">
        <div className="flex flex-col md:flex-row gap-3 md:gap-5">
          <div className="flex-1">
            <h3 className="font-semibold text-lg mb-1">{title}</h3>
            <p className="text-gray-600 dark:text-gray-400 text-sm mb-4">{description}</p>
            <div className="text-primary-500 font-medium text-sm">View demo →</div>
          </div>
          {image && (
            <div className="md:w-1/3">
              <img src={image} alt={title} className="rounded-lg w-full h-auto shadow-sm" />
            </div>
          )}
        </div>
      </div>
    </a>
  );
}