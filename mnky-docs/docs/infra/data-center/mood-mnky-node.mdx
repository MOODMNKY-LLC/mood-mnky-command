---
title: MOOD-MNKY Node Deep Dive
description: Hardware and storage profile of the MOOD-MNKY node (formerly PRO-MNKY), including its Intel iGPU media stack role.
---

## Overview

`MOOD-MNKY` (formerly PRO-MNKY) is an additional node in the cluster. It is now online and hosts the target **media stack** (ARR + Jellyfin + qBittorrent) using the node’s **Intel iGPU** for VA-API/QSV transcoding.

Hardware highlights observed on the host:

- CPU: Intel Core i9-13900KS
- iGPU device nodes: `/dev/dri/card1` and `/dev/dri/renderD128`
- Host memory: ~125 GiB

---

## Role

- Target compute + GPU/iGPU node for the MOOD MNKY **media stack**.
- Runs LXC **120** (`mnky-media-stack`) which provides:
  - Jellyfin media playback and hardware transcoding
  - qBittorrent download ingestion
  - Prowlarr + Sonarr/Radarr/Lidarr orchestration
  - Jellyseerr requests/discovery
- Storage access: TrueNAS shared media NFS dataset (mounted into the LXC as `/mnt/media`)

See: [`Media Stack (ARR + Jellyfin + qBittorrent)](/docs/infra/data-center/media-stack)`.

---

## Next steps

If you change MOOD-MNKY’s hardware (CPU, iGPU, or storage pools), re-capture the node profile by updating these facts and re-verifying:

1. The iGPU devices visible to containers (e.g. `/dev/dri/renderD128`)
2. TrueNAS NFS media mounts
3. The media-stack LXC’s Docker/Compose health
